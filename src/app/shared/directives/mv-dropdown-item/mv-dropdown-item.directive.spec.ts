import { MvDropdownItemDirective } from './mv-dropdown-item.directive';

describe('MvDropdownItemDirective', () => {
  it('should create an instance', () => {
    const directive = new MvDropdownItemDirective();
    expect(directive).toBeTruthy();
  });
});
