import { MvOverlayDirective } from './mv-overlay.directive';

describe('MvOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new MvOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
