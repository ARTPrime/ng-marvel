import { MvGalleryItemDirective } from './mv-gallery-item.directive';

describe('MvGalleryItemDirective', () => {
  it('should create an instance', () => {
    const directive = new MvGalleryItemDirective();
    expect(directive).toBeTruthy();
  });
});
