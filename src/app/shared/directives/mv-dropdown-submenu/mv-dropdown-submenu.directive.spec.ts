import { MvDropdownSubmenuDirective } from './mv-dropdown-submenu.directive';

describe('MvDropdownSubmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new MvDropdownSubmenuDirective();
    expect(directive).toBeTruthy();
  });
});
