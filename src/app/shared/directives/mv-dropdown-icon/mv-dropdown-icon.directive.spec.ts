import { MvDropdownIconDirective } from './mv-dropdown-icon.directive';

describe('MvDropdownIconDirective', () => {
  it('should create an instance', () => {
    const directive = new MvDropdownIconDirective();
    expect(directive).toBeTruthy();
  });
});
