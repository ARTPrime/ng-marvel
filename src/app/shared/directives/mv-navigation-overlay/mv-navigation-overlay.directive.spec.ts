import { MvNavigationOverlayDirective } from './mv-navigation-overlay.directive';

describe('MvNavigationOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new MvNavigationOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
